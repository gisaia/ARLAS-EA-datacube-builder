gs:
  bucket: "${GS_OUTPUT_BUCKET}"
  api_key:
    type: "service_account"
    project_id: "${GS_OUTPUT_PROJECT_ID}"
    private_key_id: "${GS_OUTPUT_PRIVATE_KEY_ID}"
    private_key: "${GS_OUTPUT_PRIVATE_KEY}"
    client_email: "${GS_OUTPUT_CLIENT_EMAIL}"
    client_id: ""
    auth_uri: "https://accounts.google.com/o/oauth2/auth"
    token_uri: "https://oauth2.googleapis.com/token"
    auth_provider_x509_cert_url: "https://www.googleapis.com/oauth2/v1/certs"
    client_x509_cert_url: "https://www.googleapis.com/robot/v1/metadata/x509/${GS_OUTPUT_CLIENT_EMAIL}"
